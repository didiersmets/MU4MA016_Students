#include <stdio.h>
#include <math.h>


int main(){

   /* Question 1
   
    FILE *f = fopen("test-tp0.txt","r");
    if (f==NULL){
        printf("erreur\n");
        return 0;
    }

    char buffer[256];
    char *res = fgets(buffer, 3, f);

    while (res!=NULL){
        printf("%s",res);
        res = fgets(buffer, 3, f);
    }
    */


    float xmin = -6*M_PI;
    float xmax = 6*M_PI;

    
    return 0;       
}